# UXレビュー: 思考の芽 Ver1.03 添付レビュー対応

対象: `965a60423b3dc1f0cab593d0b847be947796077f`（基点 `6d7a06f`、前回レビュー対象 `3114cfd` の限定修正後）。

主要動線: 目的を選ぶ → 依頼文を作って外部AIへ渡す／手持ちの返答を貼る → 回復候補を選んで内容を確認 → 必要なカードを追加してUndoできる。補助対象はカードの開閉と理由を含むまとめ。

スタイル世界観: paper/greenのノートと自由配置ボード。独立したdesign-labスタイル憲章は見当たらず、既存の `--paper`、`--canvas`、`--green` 等のプロジェクトトークンへ合わせている。

## findings

- [G17] 重 | 箇所: `src/index.html:54–55` の「不具合・感想を送る ↗」と公開投稿の注意書き。入口はノートメニュー内にあり、欠如ではない。一方、遷移先はGitHubの公開Issueで、`PLAN.md:86,97` と `DESIGN.md:165` が記録するとおり、GitHubアカウントが必要で、匿名受付・任意返信メール・保存箱・即時通知とその受信検証は未実装。個人の考えを扱うアプリでは公開投稿だけだと伝えにくい内容があり、一般公開前に閉じたフィードバック経路を整える必要がある。これは既知の残件で、現行の静的・認証なし・クラウドなしという仕様では完成扱いしない。

## 確認根拠

`QA.md` と `docs/review-followup-2026-09-26.md` に、build成功、構文42ファイル、153 tests成功（fail/skip 0）が記録されている。下書き原文の一致、依頼文だけの復元・破棄、2タブ競合、部分追加後の再読み込みとUndo、通常形式JSONの残りを追加して完了・下書きを削除する操作を確認済み。失敗・競合時に返答を保持し、追加先の相違を拒否する。

前回3114cfdで検出したG11は965a604で解消。`dist/ai-transfer.mjs:56,75,92,97,114–115` の検証条件とJSONキー契約を保ったまま、必要項目・`children`・`branches`・`version` の形式エラーを人向けの案内へ変更し、候補がない時に同じ説明を重ねていた導入文も `dist/app.mjs:2342` で隠す。親セッションの実画面確認では `{version:2,kind:'notebook',title:'形式確認',children:[]}` に対し「ノートに含める考えが見つかりません。返答の形式を整える依頼文を使って、AIに修正を依頼してください。」を一度だけ表示し、原文は完全一致で保持、候補・追加はなし。前回のG11 findingは今回の集計に含めない。

画面証拠は `docs/v1.03-recovery-mobile.png`、`docs/v1.03-partial-resume.png`、`docs/v1.03-summary-mobile.png`、`docs/v1.03-goal-collapse.png`。AI作業は目的・依頼・返答・確認を一画面一段階にし、通常の折り畳み情報と候補外文章はdetailsへ畳んでいる。長いAI確認画面4寸法、編集欄8寸法の実スクロールで固定見出し・閉じる操作・下部CTAと横幅を確認している。要約は関連カードの切替を出力へ反映する。折り畳みはカードに統合した矢印と件数で隠れる総数を示す。

C2の現行測定は160×96カードで、100%時の押下領域43.99775px・最小間隔7.99074px、86.4%時43.9956px・8.001747px。44px/8pxとの差はブラウザーの小数丸め未満で、今回は不具合として数えない。旧v19の0.19pxおよび修正前の2px記録は過去状態として扱い、現行へ転記していない。CSS色トークンから計算した代表コントラストは本文/紙12.19:1、補助文字/紙5.81:1、補助文字/wash 4.95:1、緑/紙6.29:1。

## 未確認範囲

実スマホでのタッチ・IME・ソフトキーボード、スクリーンリーダーの実発話、ChatGPT/Gemini/Claude各サービス固有の往復、GitHubへの実投稿・通知受信は未確認。画面幅とブラウザー操作の記録を実機確認と同一視しない。Ver1.03候補はレビュー時点で未デプロイ。

## 集計

致命 0 / 重 1 / 中 0 / 軽 0

VERDICT: PASS
