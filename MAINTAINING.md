# コードを変更する場合

## 変更箇所の入口

- `dist/app.mjs` は画面の操作を、保存と履歴につなぎます。通常の内容変更は `transaction`、文章の入力は `editNode` を確認してください。
- `dist/model.mjs` はノートの構造と外部入力を検証します。座標と分類枠の処理は `dist/board-model.mjs` にあります。
- `dist/board-view.mjs` は描画とポインター操作を担当します。ドラッグ途中の表示と、確定して保存する操作を分けています。
- `dist/storage.mjs` はブラウザ保存を、`dist/history.mjs` は戻す／やり直す履歴を担当します。どちらも画面の部品を直接操作しません。

## 変更時に保つ条件

読み込んだJSONとAIの回答は、画面に使う前に検証します。
文字列はHTMLとして解釈せず、`textContent` または入力欄の `value` に設定してください。

保存に失敗しても、入力中のノートを初期化しません。
古いタブからの上書きは世代比較で拒否し、バックアップを読み出した状態でも勝手に上書きしません。
保存形式を変える場合は、既存ノートの読み込みとJSONの往復を確認してください。

共有デモには独立したworkspaceを使います。
閲覧しただけで本人のノートを変えず、コピー操作のときだけ別のIDで追加します。
AIの提案も、受け取りだけでは本体に入りません。
本人が画面で承認したものだけを追加します。

履歴に登録する内容のスナップショットは、後から書き換えないでください。
表示位置だけの履歴にはノート本文を持たせず、共有デモでも内容が変わらないようにしています。
文字編集の履歴をまとめる時間と、ドラッグ終了時に確定する処理は別に確認してください。

## テストと整形

動作とテストに必要なのはNode.js 22以降です。
アプリ実行用の外部パッケージをインストールする必要はありません。

```sh
npm test
npm run dev
```

テストは構造、保存、配置、履歴、ズームの処理を確認します。
実ブラウザの操作を代替するものではないため、画面の変更時には保存と再読み込み、取り消しとやり直し、PCとスマホ幅の表示も確認してください。
現在の未確認範囲は [QA.md](QA.md) に記録しています。

JavaScriptの整形には、任意でPrettier 3.6.2を使えます。
以下のコマンドは初回に整形ツールを取得するため通信しますが、公開アプリの依存には追加しません。

```sh
npx --yes prettier@3.6.2 --check "dist/*.mjs" "tests/*.mjs" server.mjs
npx --yes prettier@3.6.2 --write "dist/*.mjs" "tests/*.mjs" server.mjs
```

整形条件は [.prettierrc.json](.prettierrc.json) を共有します。
JavaScriptの改行は [.gitattributes](.gitattributes) でLFにそろえ、Windowsでも同じ結果にします。
整形の後も `npm test` を実行してください。

## 公開版と履歴

`dist/` は直接編集する公開ソースです。
画像、CSS、HTML、JavaScriptだけで動き、ローカルの `server.mjs` は開発用です。
公開URLと配布したコードの対応は [LAUNCH-STATE.md](LAUNCH-STATE.md) に記録します。
ノート本文や秘密情報をGitHubへ追加しないでください。

9月13日23:58:26 JSTの機能版は [9cda3fd](https://github.com/toki0001/shiko-no-me/tree/9cda3fd36e2041a8162c5c50b713f8ed34459dbd) で確認できます。
9月14日0:00:09の次の更新は、配布記録と検証記録のみでした。
今回のコメントと整形も9月14日の変更として残し、過去のコミット日時や履歴は書き換えません。

[公式応募規定](https://progedu.github.io/webappcontest/2026/summer/index.html) は審査期間中のコード変更を禁止していませんが、提出コードと公開サイトの動作を一致させる必要があります。
この条件は応募フォームの提出期限を延長するものではありません。
