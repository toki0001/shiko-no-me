# 初回表示の調査（2026-09-15）

## 改善前の実測

公開v6の共有デモをWindows上のCodex内蔵Browserで確認した。端末・回線を固定した少数の観測で、全利用者の値ではない。

| 計測 | HTML最初の応答 | 最初の文字表示（FCP） | DOMContentLoaded |
| --- | ---: | ---: | ---: |
| 開いていたページの既存記録 | 2,291 ms | 5,420 ms | 5,361 ms |
| ブラウザキャッシュを一時的に無効にして再読み込み | 2,319 ms | 4,744 ms | 4,838 ms |

後者ではHTML受信後のstyles.css待ちが約2,321 ms、app.mjs待ちが約1,747 msだった。ファイルの転送容量はページと資産全体で数十KB程度であり、大容量の画像・フォント・外部UIライブラリはない。レイアウト16 ms・スタイル再計算29 msという観測もあり、この再現では描画計算より通信待ちが支配的だった。

HTTP単独の新規接続ではHTMLの最初の応答に約1.1〜2.1秒かかった一方、接続を使い回した12回のGETはほぼ35〜47 msだった。配信側・接続経路で大きく変動するため、初期応答時間をアプリだけで解消できるとは断定しない。

## 今回の変更

- CSS3本と起動用app.mjsをHTML内へまとめ、HTMLを受け取った後の独立した4本の通信待ちを除く。
- 依存する6モジュールはHTML冒頭のmodulepreloadで並行取得する。
- CSSの適用順とアプリの処理は変更しない。元ソースを読みやすい個別ファイルで保持し、Node標準機能だけの生成処理を用意する。
- HTMLの編集元はsrc/index.html、公開用dist/index.htmlはnpm run buildで生成する。生成物が古い場合はテストが失敗する。
- 保存形式・ノート内容・ホスティング先・公開範囲・認証やセキュリティ保護は変更しない。

CSSの通信が初期描画を止める仕組みと、インライン化によるHTML容量増加のトレードオフは[Chromeチームの解説](https://web.dev/articles/extract-critical-css)を参照した。今回の目的は容量の削減ではなく、待ち時間の大きい通信を減らすこと。

## 検証

65テスト成功。生成HTMLと元CSS/JSの一致、依存モジュールの事前取得、CSS順序、置換文字列の保持、改行差分、危険な終了タグの拒否を検証した。ローカル実ブラウザで25カードの描画とデモの次の段階への操作、画面を確認した。

公開後の計測は配布完了後に記録する。
