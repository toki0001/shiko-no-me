# 思考の芽 — 制作状況

## Status (2026-09-13)

ブラウザ単独版の初版を実装し、本人限定URLへ配布済み。一般公開・GitHub公開・応募は未実施。正式な表示・操作確認を通すまでは、コンテスト提出完了・実機確認済みとは扱わない。URLと配布コードの正本はLAUNCH-STATE.md。

同日、本人がフリーボード・端末別の分岐操作・線の色・分類枠に合意。ボードへの変更を実装し、互換性とジェスチャーの状態遷移を含め53テスト成功。ボード版の独立レビューで入力中の保護・枠の所属整合性・選択対象・フォーカス復帰を修正し、最終差分確認はPASS（残指摘0）。本人限定URLへの更新は進行中。Browser接続候補は今回も空で、実画面確認は継続して未実施。

## 実装済み

- ノート作成、考え・理由の編集、採用／保留／見送り、親子・兄弟の追加。
- 枝の上下移動、階層変更、折りたたみ、部分表示、削除と取り消し。
- ブラウザ内自動保存、世代バックアップ、保存失敗表示、別タブの競合検知。
- JSON書き出し・追加読み込み。元のMCP試作のツリーJSONも読み込める。
- AIへの依頼文コピー、範囲指定、回答JSON検証、個別承認・見送り。固定サンプルを明示した取り込み体験。
- 外部依存なしの任意WebMCP（読取・保留のみ）。
- カードを自由配置するボード。PC四辺の＋／スマホダブルタップで分岐、重複しない新規位置、ドラッグ、パン、拡大縮小、一覧との切替。
- 接続線の任意色、名前・色付き分類枠、複数選択、枠と中身の移動、サイズ変更による所属更新。分類は親子・採否と独立。
- 旧ノートの空間情報補完、枠を含むJSON入出力、ドラッグ1回単位の履歴、未確定入力の離脱警告。

## 確認状況

- ロジック：Node標準テスト53件成功。初版の構造・採否・保存・JSON検証に加え、座標の互換性・衝突回避・枠の所属整合性・ジェスチャーの状態遷移を含む。実DOMや実機イベントの再現ではない。
- 配信：開発用HTTPのトップページが200。公開ファイルの相対参照とJS構文を確認。
- 視覚・操作：操作用ブラウザの一覧が空で、接続できていない。PC・スマホ幅のスクリーンショット、実クリック、タッチ領域・横はみ出しの実測は未確認。
- WebMCP：ブラウザ側のツール登録・有効入力・無効入力の契約テストは未確認。ソースとドメインテストで代替したとは扱わない。
- 独立レビュー：初版・ボード版とも修正後にコードレベルでVERDICT: PASS（残指摘0）。画面を見た審査ではない。記録はQA.md。公開フィードバック窓口は本人限定配布の対象外だが、一般公開前の必須ゲートとして残す。

## 引き渡し前後の優先事項

1. 独立レビュー指摘の修正と回帰テスト追加は完了。次の変更でも保存・承認・モバイルの回帰を確認する。
2. 本人限定の配布URLは用意済み。ブラウザ接続を回復したら、実操作と画面確認を完了する。
3. 本人が新しいノートを作り、思考の枝を伸ばす流れを一周して確認する。
4. コンテスト提出前に、公開用GitHub、一般公開URL、サムネイルなどフォームの後続項目、フィードバック窓口、PC・スマホChromeの動作を確認する。公開・応募は本人の指示なしに行わない。

提出期限の最新確認は [公式募集要項](https://progedu.github.io/webappcontest/2026/summer/index.html) で行う。ルール確認の記録は元の試作のPLANにある。
