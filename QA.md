# 検証記録

## ボードの判断色（2026-09-26、31da1ad）

ロジック: build・構文42ファイル・153テスト成功。配布コミットの[GitHub Actions](https://github.com/toki0001/shiko-no-me/actions/runs/36247344251)もcompleted/success。採用・保留・見送りだけを既知のclassに対応させ、保存データや判断処理は変更していない。

視覚・実操作: Luna Max実装後、Astraが初版の細い帯の角丸を差し戻し、カード全体の角丸に沿う上辺へ修正。1280×720・390×844で通常/選択を確認。比較画面と上辺の実測色が一致（採用rgb(50,106,74)、保留rgb(170,120,62)、見送りrgb(165,170,163)）。四つの判断を順に切り替え、未判断で色が消えることを確認。カード寸法と各操作の相対位置の修正前後差は0.0001px未満。採用カードの開閉前後の位置は不変、開閉の押下領域44px相当・追加点/四隅との最小間隔7.99px。Undoで元の未判断に戻ることも確認した。

共通ボードCSSの回帰として、編集欄を390×844・1280×720・695×901・768×1024・844×390・601×901・599×901・568×321の実測8寸法で上下端までスクロールし、固定見出し/閉じる到達性/横はみ出しを確認。ブラウザの寸法丸めで600指定は601になったため599で境界の反対側を確認し、回転中の過渡状態は落ち着いてから再測定した。console error 0。独立Luna Maxは差分1b52aaa..31da1adとPC/スマホ/折りたたみ画像を確認してPASS。実機タッチは未検証。

確認画像: [PC](docs/board-status-desktop.png)、[スマホ幅](docs/board-status-mobile.png)。公開状態はLAUNCH-STATE.md。

## Ver1.03 添付レビューへの対応（2026-09-26）

ロジック: build、JavaScript構文42ファイル、153テスト成功（fail/skip 0）。原文を保持する返答の回復、別対象の拒否、下書きの容量・破損・別タブ競合、追加済み部分の保存、関連カードを含む要約、開閉時の全件数を確認。Luna Maxの実装をAstraが差分と実操作で確認し、通常形式の部分追加でも再開用の相談先情報を保持する修正を加えた。

独立レビューで、形式エラーに機械用の項目名が出る箇所も検出。検証条件を変えず、日本語の原因と形式修正の次の操作を案内する文へ変更。候補がない場合の同じエラー説明の二重表示も除いた。不正な新規ノート返答を実画面で拒否し、原文を保持して案内することを確認した。

視覚・実操作: PCと390幅で文脈別のAI入口、原文メモ、複数候補、別用途への切り替え、部分追加、Undo、入力直後の閉じる操作と再読込、下書き再開・破棄を確認。依頼文だけでも保存され、破棄後は再出現しない。複数候補の部分追加後に再読込すると追加済み項目が無効になり、Undoで取り消した候補は再び選べる。別タブ競合時には先の保存を保持し、入力を消さずにカード追加を拒否する。

長いAI確認画面は1280×720・390×844・844×390・601×901、既存の編集パネルは8寸法で本文の上下端まで実スクロールし、固定見出し・閉じる操作・横はみ出しなしを確認。AI操作は最小44px相当。要約の関連カード切替は本文に反映され、PCと390幅で横はみ出しなし。最小カード160×96の100%と86.4%表示で、開閉ボタンは44px相当、四方向の追加点・四隅との間隔は約8px。開閉前後のカードの画面位置は不変。隠れた4/8/72枚を実表示で確認した。旧v19の0.19pxの間隔制約は今回修正した。

詳しい対応表・実測・画像は [添付レビューへの対応](docs/review-followup-2026-09-26.md)。実スマホのタッチ・IME・ソフトキーボード・スクリーンリーダーは未検証。ノート保存と下書きの追加済み記録は別の保存であり、その短い間にブラウザを強制終了した場合まで重複防止を保証しない。公開状態はLAUNCH-STATE.mdに分離する。

[独立UXレビュー](docs/ux-review-followup-2026-09-26.md)は最終コード965a604でPASS。匿名の非公開フィードバック受付は既知の残件として記録。最終コミットの[GitHub Actions](https://github.com/toki0001/shiko-no-me/actions/runs/36245233276)もcompleted/success。

## AI画面・文言・カード開閉の再整理（2026-09-26追加）

ロジック: build、構文35ファイル、131テスト成功（fail/skip 0）、git diff --check成功。開閉のキーボード操作と読み上げ名を回帰テストへ追加。UIの段階・主操作数・フォーカスはソース文字列の照合ではなく、実ブラウザー用 `scripts/check-ai-workflow.mjs` で確認した。

視覚・実操作: PC/390幅で目的選択、コピー、返答の確認、部分選択して3カード追加、Undo、新規5カードノート作成、下書きとサンプルから戻った入力の保持、不正入力・重複拒否からの案内を確認。4寸法の長いAI確認画面、8寸法の既存編集欄を実スクロールして固定見出しと閉じる操作を検証。カードはEnter一回で閉じ、再度Enterで開く。20pxの単色矢印、44画面pxの押下領域を目視・測定した。最小カード160×96の85%表示では追加点との間隔が0.19pxとなる制約が残る。実機タッチは未検証。

詳細と確認の限界は [今回のレビュー記録](docs/ux-simplification-2026-09-26.md)。PC/スマホ幅の画像を同じdocsに保存した。公開状態はLAUNCH-STATE.mdを参照。

## 目標デモのキーボード移動（2026-09-26追加）

Astraの追加確認で、全体図に覆われたボードへTabフォーカスが入る問題を検出。専用の可視フォーカス確認スクリプトで修正前の画面が失敗することを確認し、Luna Maxが全体図の背後だけをinertにする最小修正を実装した。

ロジック: build、構文34ファイル、既存130テスト成功。視覚/実操作: PCでノート→Tabが出典リンクへ移り、要素をEnterで開くとそのカードへ、戻ると元の要素ボタンへ復帰。390幅も一覧の選択行と要素ボタンで同じ動作を確認。全体図からコピーすると73カード・選択主題へフォーカスし、ボードと倍率操作のinert解除を確認。コピー後のスマホ編集モーダルは背景をinertにし、閉じると解除される。実機キーボード・スクリーンリーダーは未検証。

## Ver1.02 AI取り込み・目標デモ（2026-09-26）

ロジック: Node.js 22.16.0でJavaScript構文33ファイル、HTML build、全130テスト成功（fail/skip 0）。AIの階層取り込み、入力制限、別対象・重複拒否、部分選択、失敗時の一括不変性、73ノードのデモ、枝の開閉・分類枠の絞り込み、画面回転後のfitを追加検証した。

Astraレビュー: Luna Maxの実装後、差分と40入力ケースを独立確認。深い枝の重複署名、AI出典、会話用の自由質問、コピー操作の位置、スマホ末尾の隠れ、回転後の中心ずれ、デモコピー後のWebMCP登録を修正させた。実装者とは別のLuna Maxによるソース・画像・操作記録のUXレビューはPASS。形式違いの案内からモードを切り替え、貼り直してプレビューできることも確認した。

視覚・実操作: 内蔵BrowserでPC/390幅のAI依頼文コピー、サンプルと実Luna返答のプレビュー、一部選択、追加、Undo/Redo、保存後の再読込を完走。Markdown・説明付きJSON・不正入力・下書き切替・深さ31のプレビューも確認。目標デモは全体→要素→行動、スマホ一覧、横向き、73カードのコピー、元の企画デモを確認した。既存編集欄は8画面寸法で固定見出し・本文の実スクロール・閉じる操作・背景の操作制御を再検証。

WebMCP: 内蔵Browserから読み取りと提案追加を実際に呼び出した。選択した枝と祖先見出しだけを返し、提案は承認待ちで本体不変。画面で追加・Undoを確認。閲覧デモをコピーすると提案ツールも使える。

詳しいケース・測定値・修正履歴は [AIレビュー記録](docs/ai-review-2026-09-26.md)。実スマホ・日本語IME・ソフトキーボードと各外部AIサービス固有の動作は未確認。公開結果はLAUNCH-STATE.mdへ分離する。

## コード品質ゲートと保存境界（2026-09-26）

`.github/workflows/ci.yml` を追加。GitHub ActionsでNode.js 22のJavaScript構文確認、配布HTMLのbuild、Nodeテスト、生成後の `dist/index.html` 差分確認を実行する。ローカル再現コマンドはREADME.mdとMAINTAINING.mdに記載。

回帰テストでは、保存容量不足に加えてバックアップキーへの書込み失敗、読み取り拒否、破損した最新保存に対して復旧内容を保存しようとしても元データとバックアップを上書きしないことを確認する。JSON追加読み込みは有効なノートの後ろに不正ノートがある場合も既存workspaceを変えず、AI回答の複数案検証も途中まで反映しない既存テストを維持する。

ロジック: Node.js 22.16.0で構文27ファイル成功、HTML build成功、98テスト成功。build後の `git diff --exit-code -- dist/index.html` も成功し、生成HTMLの更新漏れなし。`git diff --check` 成功。アプリの画面・挙動は変更していない。

Astraレビュー: Luna（Max）の完了後、差分と失敗時のデータ保持を独立確認し、上記コマンドを再実行して成功。別コピーでバックアップ書込み失敗を無視する変更と破損検証を省く変更を入れ、対象テストがそれぞれ失敗することを確認。構文不正も構文チェックが検出した。公開用ファイルにこれらの故障は入れていない。レビューの追加修正要求なし。

視覚・実操作: ローカル4196のテスト用ノートで、問いと案の追加、採用、取り消し、やり直し、保存後の再読込を確認。1280×800のボード/右編集欄と390×844の比較/下部編集欄を目視し、documentの横幅は各1280/390pxで横はみ出しなし。実スマホ・日本語IME・ソフトキーボードは今回未検証。サムネイルJPEGの名称・副文・判断ラベルを目視確認。公開と画像URLの確認結果はLAUNCH-STATE.md。

## スマホ縦限定の表示方式（2026-09-20）

ロジック: ビルドと96テスト成功、独立レビューPASS。JS/CSSの条件を600px以下かつ縦長へ統一。ノート一覧と編集欄を開いたままスマホ縦へ変更する際の二重inertをレビューで検出し、一覧を先に閉じることで修正。

実測: 695×734・768×1024・602×900・844×390・568×320・1280×720で右パネル/背景操作可、599×900・390×844で下部モーダル/背景操作不可。追加バーとmodal/inertの条件一致、本文と見出しの境界・横はみ出しを検査。695と390幅では先頭/末尾の実スクロールも検証。695幅は画像で確認。568横は追加バー55.66px高、保存表示との重なりなしを確認。回転しても編集欄を維持し、一覧が閉じmainのinertが解除され×へフォーカスすることを実操作で確認。実機タッチ/ソフトキーボードは未確認。

## 編集パネル見出しの浮き（2026-09-20）

原因: 本番のパネル上端123.65pxに対して見出し155.63px、差31.99px。外側paddingとsticky見出し・全体スクロールの混在。固定行/本文スクロールへ分離し、競合CSSを撤去。旧本番に新位置チェックを実行すると32pxの浮きで失敗（読み取りのみ）。

ロジック: ビルド・既存96テスト成功。独立コードレビューPASS。検証スクリプトへ「実際に本文がスクロールしたこと」のassertも追加し、短い内容での空振りを防止。

視覚/実操作: ローカルPC通常幅、1150/941/940付近、768/390幅、844×390の低い画面で本文末尾/先頭への移動とヘッダー位置・44px相当の×の到達性・横はみ出しを数値確認。PC見出し差0px、下部パネルは枠線分約0.83pxのみ。本文移動量69〜608px。線/枝の詳細展開、比較画面から別カード選択でscrollTop=0、末尾の×で閉じることも確認。PC/390幅の実画像はdocs/inspector-header-desktop.jpg / inspector-header-mobile.jpg。実機タッチ/キーボード表示は未確認。

再発防止: scripts/check-inspector-layout.mjsとdocs/inspector-regression.md、当repoのAGENTS.mdにUI変更時の確認を登録。共通owner-rulesのG2/P8へも反映。CoHoursの過去の浮き報告は台帳で確認したが、同じ実装原因だったとは断定しない。

## 背景Aを確定（2026-09-20）

ロジック: ビルド成功・既存96テスト成功。比較用HTML/CSS/JSと初回表示の特例の削除を差分確認。低影響の表示変更につき新規単体テストなし。

視覚: ローカルPC通常URLと旧比較URL（dots=c）で背景の算出alpha=0.15、間隔24px、比較バーなしを確認。通常URLのスクリーンショットを確認。本人も公開比較画面でAを選択済み。今回の公開後ブラウザ再検証は行わない。

## 背景3濃度の比較（2026-09-20）

ロジック: 既存96テスト成功。背景だけの低影響変更につき新規単体テストなし。URLの選択値は固定3候補に限定。

視覚/実操作: ローカルでA/B/Cを実クリックし15/25/35%の算出色とaria-pressed/URL、Undo無効のままを確認。3案すべてPCでスクリーンショット確認（docs/background-preview.jpgはC）。Aへの再読込保持、通常URLはバー非表示で25%、390幅scrollWidth390・3ボタン44px高を確認。実機の液晶での好みは本人の比較待ち。

## ドット方眼・タブレット配置・枠名編集（2026-09-20）

ロジック: 96テスト成功。インライン名変更のtrim/一度だけ確定/空欄拒否/取消/IME中拒否/閲覧専用の選択を3テスト追加。独立コードレビューVERDICT: PASS。

視覚/実操作: ローカル4193のPCで枠名クリック→直接入力→Enter、blurして隣の編集ボタンが1クリックで開くこと、Escape取消、Undo/Redo、保存後の再読込を確認。空欄はCtrl+A/Backspaceで作り、Enterで未確定のまま案内が出ることを確認。枠名ドラッグで枠とカードがともに40×30移動し、編集欄が出ないことを測定。1280/768/601pxでPCバー左約12px・下約12px、600以下はスマホバー。390幅のモバイルバーは幅約366pxで名前編集も開く。タブレットのドット背景と編集欄の確認画面はdocs/dot-notebook-tablet.jpg（保存状態表示の最終修正前の記録）。

実タッチ/実機IMEは未検証。ツールのtouch event送信は非対応で、タッチ完走の確認とはしない。ブラウザ幅の変更は確認後に解除。

## 四つ角のサイズ変更（2026-09-20）

ロジック: 全93テスト成功。4角の対角固定・最小/最大寸法・枠所属・接続線・整列参照・追加空き位置・保存/JSONコピー・不正寸法・ズーム換算・最終pointerup座標・取消を検証。独立コードレビューで小倍率のタッチ領域重複を修正後、VERDICT: PASS。独立した視覚レビューではない。

視覚/実操作: ローカル4193でカード/枠それぞれ4角を実ドラッグ。カード右上224×116→324×176、全8角で位置/寸法の変化を測定。Undo/Redo、保存済み表示後の再読込、枠右上の矢印キー操作とフォーカス維持、ドラッグ途中Escapeで元の寸法への復元を確認。枠の右上ハンドルと編集ボタンは約6px離れ、透明背景。1280×720の画面をdocs/resize-corners.jpgに保存。390幅＋touch emulationで最小160×96カード・25%表示の中央はcard-body、角は非表示、横幅390。実スマホの指操作は未確認。

## Ver1.01公開（2026-09-20）

ロジック: 公開ラベルの「確認版」を外して再ビルド、全88テスト成功。配布コード673666ba38dbae49830289e2d13c59566fa467fbをGitHub/Sites mainへpushし、14ファイルの梱包を確認。Sites version 10の公開成功は11:52:38 JST。

視覚・実操作: キー追加時のローカルPC/390幅での確認を引き継ぐ。今回の公開後のブラウザ操作・HTTP配信資産の一致確認は追加実施していない。公開タブへの表示依頼はqueued。実スマホ/IME/Macの未検証は継続。

## 初回表示改善（2026-09-15）

ロジック: 65テスト成功、独立レビューPASS。配布HTMLの生成元一致・CSS順序・モジュールの先読み・終了タグ拒否・生成物の更新漏れを追加検証。既存の保存と履歴の処理は変更していない。

視覚・実操作: 内蔵BrowserのローカルPC幅961でデモのスクリーンショット、25カードの描画、ノート一覧の開閉、ズームと取り消しを確認。v7公開後も25カードとデモの次/前を実クリック。今回実スマホでは未検証。

性能: 公開デモのFCPを変更前4744/5420ms、公開後260/220msと計測。初期応答時間も約2.3秒から約0.05秒へ変わっているため、全差分をコードだけの改善とは扱わない。詳しい時系列・計測条件・未確認範囲はPERFORMANCE.md。キャッシュ無効化は最後に解除した。

## コンテスト評価項目の現状診断（2026-09-15）

評価レポートは `CONTEST-REVIEW-2026-09-15.html`。製品コードの修正・デプロイはしていない。

ロジック: ローカルHEAD `12fb659` で既存62テストを再実行し、62成功。GitHub main `c4494fd` との差分はPLANの提出報告のみで、製品ソースは同一。GitHub APIでIssue・PR・Actions実行履歴各0、mainのみを確認。ローカル16コミット、公開main15コミット。

視覚: 内蔵Browserでローカル初期サンプルを1280×720と390×844で確認。390幅はdocument scrollWidthも390で横はみ出しなし。ただし初期倍率32.1%、ルートカードの表示寸法約71.29×36.66px、元幅222px、CSS文字16pxで、変形後の文字は約5.1px相当。カードを実クリックすると85%へ拡大。初回の読みにくさ・拡大操作の発見性は改善候補。実スマホではない。

操作: 公開版のノートメニュー→使い方、およびAIダイアログを実クリックで確認。ローカル390幅で下部追加→検証用カード入力→追加→保存済み表示→再読込後の復元を確認し、取得したconsole errorは0件。公開版の既存ノートは変更していない。視覚確認用のviewport変更は最後に解除した。

未検証: 実スマホのタッチ・日本語IME・ソフトキーボード・ピンチ、第三者の初回体験・継続利用、WebMCP呼出、AIサービスとの実往復、長時間・大量データ、今回の全機能回帰確認。テスト成功をこれらの証明にしない。

## 操作の配置修正（2026-09-14）

ロジック: 全62テスト成功。上部バーのボタン順、履歴ボタンが倍率操作の外にあり一覧でも残る構造、兄弟追加ボタン削除後の参照切れを静的に確認した。既存の保存・履歴・ズームのテストも成功。JavaScript構文、整形、相対ファイル参照、git diff --checkを確認。独立コードレビューは残指摘なし・PASS。

視覚・実操作: 今回は未確認。確認用HTTPの200は取得し、プレビュー表示を依頼したがqueuedで、表示済みとは断定しない。ブラウザの描画・実寸・クリックは検証していない。細いポインターでは小さく、タッチでは44pxの操作領域を使うCSSと、狭幅では「全体」に短縮する表示をコードで確認した。

配布後: v6の成功と匿名HTTP200を確認。JS/CSSの10ファイルはローカルソースと完全一致した。HTMLは末尾に配信側のスクリプトが追加されるため完全一致チェックには失敗し、ノート入口の新属性と削除した兄弟追加ボタンの不在を個別に確認した。HTML全体の同一性や実ブラウザの表示を検証したとは扱わない。

## 保守向けコメント・整形（2026-09-14）

ロジック: 全61テスト成功。変更した14個のJavaScriptモジュールで構文を検査し、Prettier 3.6.2の整形前debug-checkと整形後checkに成功。
同ツールのAcornパーサーで変更前後を構文木に変換し、位置情報・コメント・リテラル表記などの付加情報を除いて全14個の一致を確認した。
機能変更ではなく、コメントと改行・インデント等の整理であることを確認するための検査であり、実ブラウザの操作検証ではない。
アプリ実行時・テスト時の外部依存は増やしていない。

視覚・実操作: 今回は未実施。HTML・CSSは変更しておらず、公開サイトは9月13日のv5を維持する。
v4の実トラックパッドの手触りとv5の矢印の実画面は、従来どおり未確認のまま。

以下は9月13日の各機能版の検証記録。

## 戻す・やり直す版

独立コードレビューは重大0・残指摘0・PASS。配布後にHTTP200と新しいredo/history-controlsを確認。画面の見え方を確認したこととは区別する。

全61テスト成功。内容/位置を複数回戻して同順序でやり直すこと、戻した後の新編集で進む経路を破棄すること、表示位置を内容履歴と区別すること、40段階の上限・空履歴・クリアを追加検証。文字/メモ別に600msの休止または約1秒で区切り、移動はドラッグ終了時に一段階を記録。JS構文・差分整形も確認。今回追加した矢印の実画面・実機タッチは未確認で、v3の確認を流用しない。

## ホイール・ピンチの微調整版

57テスト成功（従来54＋ズーム3）。通常ホイールで上下移動せず倍率が変わること、カーソル下のワールド座標を維持すること、小数deltaのピンチが連続し分割入力でも同じ倍率になること、行/ページ単位の補正、大きな1イベントの制限、入力欄のスクロールを妨げないことを検証。JS構文・差分整形も確認。今回の変更後の視覚・実トラックパッドの手触りは未確認で、下のv3ブラウザ確認と区別する。

## 提出直前の全画面版・共有デモ

以下が現在の確認状況。下のBrowser未接続は当時の履歴で、今回接続に成功した。

ロジック: 全54テスト成功。JavaScript構文・相対ファイル参照・差分整形も成功。企画デモ25カード・6分類枠・7段階の妥当性、毎回の独立したID、比較用の補完案の明示を検証。

実画面: 内蔵Browser、PC 961×734 / 1280×800、スマホ幅390×844。961幅でボードの実矩形は旧771.3×381.1（画面の41.7%）から961.3×677.7（92.4%）。390幅では390.5×787.3（93.4%）、documentElement.scrollWidth=390でページ全体の横はみ出しなし。

実操作: 新規ノート→右の＋→文章入力→追加→保存表示→再読み込みで復元。AI固定例2案の取り込み後もカード2枚、承認1案後に3枚、取り消しで2枚へ戻ることを確認。デモの順送り、メモのreadonly属性、デモコピー後の保存成功、コピー前のノートが一覧に残ること、一般共有ボタンの未対応表示を確認。1280幅の実デモをリポジトリ直下thumbnail.jpgに保存。

独立レビューでは、保存復旧待ちのコピー成功誤表示と、幅変更時の非表示要素へのフォーカス残留を指摘。保存不能ならコピーしないガード、保存完了後だけ成功通知、可視ボードへのフォーカス復帰を実装した。

再レビューで保存問題の解消を確認（重大0、残る中1、PASS）。一覧表示中のフォーカス残留という残指摘も、コピー時にボードへ切り替えることと、編集欄からは現在の表示へ戻すことで修正した。最後の2行の修正は主担当で確認し、独立再レビュー済みとは扱わない。

未確認: 実機タッチ・ピンチ・IME・スクリーンリーダー。内蔵Browserのタッチイベント配送は非対応だったため、一度の失敗後に打ち切り、エミュレーションを無効に戻した。WebMCPツールの登録通知は見えたが、実呼出の正常・異常入力は試していない。GitHub Issuesへのテスト送信・通知受信は未実施。

配布後: 認証なしHTTP200で新CSS・共有中デモを取得。公開URLで次の考えへの移動とメモのreadonly属性を確認。GitHubのpublic/mainとroot thumbnail.jpgの存在・匿名HTTP200を確認。配布成功と公開範囲の正本はLAUNCH-STATE.md。

## ボード版（今回の変更）

Node標準テスト53件成功。旧32件に加え、旧保存に位置を付ける互換性、四方向への衝突しない配置、既存カード位置の維持、色と採否の独立、枠移動・サイズ変更・所属更新・枠削除、座標や色や参照の拒否、枠ID再採番を含むJSON往復、AI承認後の配置と保存失敗を検証した。枠の所属が実際の内包と異なるJSONは拒否し、通常追加・AI追加とも枠内で生まれたカードが枠と一緒に移動する回帰テストも含む。

ジェスチャーの状態遷移はNode上で直接メソッドを呼ぶ単体テスト。タッチ2回の組を繰り返す場合、PCクリックと複数選択では枝を作らない場合、ドラッグと別カードのタップ、ピンチ後の残り指を検証。実ブラウザのイベント配送・ヒット領域・pointer capture・描画・キーボード・IMEを再現したテストではない。

独立コードレビュー1回目は、IME変換中の操作、ボード再描画のフォーカス、複数選択／枠選択時の編集対象、枠の所属不整合を指摘（重3・中1、FAIL）。変換確定前の操作を拒否し、選択対象を限定し、論理IDによるフォーカス復帰と幾何学的内包の検証・追加経路同期を実装した。

2回目は前4件の解消をコードで確認しPASS（中1件）。残ったノート一覧・部分表示の戻り先・AI提案の再描画後のフォーカス復帰も修正。3回目の最終差分レビューは残指摘0・`VERDICT: PASS`。別エージェントによる読み取り専用のコード確認であり、実DOMでのフォーカス検証ではない。

最終コードで53テストを再実行し成功。全JavaScriptモジュールの構文確認、`git diff --check`、公開資産・テスト・配信スクリプトの秘密鍵パターン検査を実行した。秘密鍵パターンの一致なし。

操作用Browserを正規の接続手順と案内された一覧確認で再試行したが、候補は空（`[]`）。初回プレビュー要求もqueued。PC・スマホ・横向き・実クリック・WebMCPは未確認のまま。初版とボード版のレビューは別記録で、いずれも視覚審査ではない。

## 初版のロジック

Node標準テスト32件成功。構造・階層操作、選択した提案だけの承認、二重承認防止、再取り込みの重複防止、別ノート・削除済みの枝への提案拒否、JSON往復、旧MCP形式、保存バックアップ、容量不足、破損、古いタブからの保存拒否を含む。静的ファイル参照・外部依存ゼロ・ダイアログの名前を検査した。

`node --check` でJavaScriptモジュールの構文を確認。開発用HTTPでトップと全CSS/JSが200、適切なContent-Typeを返すことを確認した。

## 独立したコードレビュー

1回目は保存待ちの見え方、入力のたびに全体処理を行う負荷、readonly textareaの文字サイズ、ダイアログの名前、モバイルのフォーカス隔離、色だけの選択表示、上部safe-areaを指摘した。

修正後の2回目は、コードレベルで残指摘0・`VERDICT: PASS`。保存は300ms後にまとめ、フィールド編集時の処理を軽くした。サイドバーとモバイル編集欄に背景のinert、Tabの制御、閉じた時のフォーカス復帰を追加した。選択中は文字でも示し、上部・左右のsafe-areaもCSSに反映した。

公開時のフィードバック窓口については、今回が本人限定配布のためレビューの適用対象外。一般公開する前の必須ゲートとしてPLANに残している。

## 視覚・実操作の未確認

Browserの接続候補が空で、実際のブラウザへ接続できなかった。以下を確認済みとは扱わない。

- PC、360/390/412/480/600px級、横向きの見た目・はみ出し・タッチ領域。
- ノートの作成から編集・保存・再読み込み・取り消し・書き出し・再取り込みまでの実クリック。
- 日本語IME、実機キーボード表示、スクリーンリーダー、ネイティブダイアログのフォーカス。
- WebMCPツールの登録・正常入力・異常入力を、対応ブラウザから呼ぶ契約テスト。
- 認証後の配布ページ描画。配布成功はSitesの最終状態で確認し、認証なしのHTTP取得は401だった。

この記録のコードレビューPASSや53件のテストで、視覚・実機確認を代替しない。
# カード編集改善の確認（2026-09-15）

- ロジック: 69テスト成功。マウスのダブルクリックは編集、touchは従来の子追加、共有デモは読取専用。Shift・複数選択・別カード連打・ドラッグ・入力拒否時の誤作動を検証。
- 内蔵Browser実操作: 1280×720で新しい検証用ノートと2カードを作成。ダブルクリック→本文/メモ編集→別カード単クリック→元カードへ戻る→保存表示→再読込で同じ内容を確認。余白クリック後も開状態を維持。右側のスクロール324pxでもボード倍率100%を維持、カード切替でスクロール0へ。×で閉じ、F2で再開。
- 寸法/画面: ボード約920px・編集欄約360pxで境界一致。選択文字0、選択枠あり。390×844への切替で下部編集欄とaria-modal/background inertを確認、ページ横幅390px。実画面スクリーンショットを確認済み。
- 限界: 実スマホのタッチ/ピンチ、日本語IME実入力、スクリーンリーダーは未確認。独立エージェントレビューは今回未実施。

# 整列ガイドの確認（2026-09-16）

- ロジック: 全74テスト成功。倍率0.25/0.5/1/1.75で6px判定、端/中心/異なる大きさの整列、Alt解除、対象なし、非表示/自分自身の除外、複数カードの間隔維持、最終ポインター位置の確定、取消時の非保存を追加検証。
- 視覚・実操作: 内蔵Browserの既存検証用ノートで、カードの4pxの縦ずれが0に補正され、赤い破線が上端に出る実画面を確認。Alt移動は4pxのずれを保持し線なし。確定で線が消え、戻す/やり直すを確認。
- PC 1280×720で分類枠を作成し、枠上端がカード上端へ吸着する画面を確認。中のカードは枠から32px/64pxの位置を保ち、再読込でも同じ位置。Escape取消は位置を保存せず線を消す。
- 限界: 実スマホ/実トラックパッド、日本語IME、スクリーンリーダーは未確認。独立エージェントレビューは今回未実施。

# Ver1.01 確認版（2026-09-20）

- ロジック: 全82テスト成功（既存74＋比較モデル6＋hiddenボード復帰2）。ビルド生成一致・DOM ID/ラベル・相対モジュール・外部依存ゼロも含む。
- 内蔵Browser: 新しいport4191で初回の具体例3案を確認。既存port4187で空の問い→案追加→まとめ、追加取消、状態変更/Undo/Redo、理由の即時反映、編集後フォーカス復帰、再読込保存、ボード往復のカード位置を検証。
- コピー完了後のclipboard、summary textarea、Downloadsに保存したMarkdownの3本文が一致。完了前の即時clipboard読み取りは旧値になるため、コピー完了を待った値を証拠とする。
- PC1280×800: 3候補を横並び。390×844: 1列、横幅390px、判断ボタン約75×44px、保存表示は上の履歴列。スクリーンショットはdocs/v1.01-desktop.png・v1.01-mobile.png。
- 長いメモ90行を入力し、候補内のスクロールとキーボードtabindexを確認。未記入理由は未記入と明示。noteを専用の判断理由schemaへ変換しない。
- 共有デモ: 6候補→問い切替→3候補、全判断ボタンdisabled・案追加/例入口なし、読取専用理由表示、問い切替で旧パネルを閉じることを確認。デモコピー経路の新たな実行はしていない。
- 独立コードUXレビュー: 編集後フォーカス消失、追加取消で選択不一致、色設定の畳み込みによる接続線入口回帰を修正後PASS。別担当のデータ境界レビューで問い切替時の古い編集欄残存を修正。画面検証とコードレビューは別証拠。
- 未確認: 実スマホ/実トラックパッド、日本語IME実入力、スクリーンリーダー、一般利用者の理解速度、競合に対する好意度。公開版へは未反映。
# Ver1.01 キー操作の追加確認（2026-09-20）

- ロジック: 全88テスト成功（従来82+キー操作6）。新旧キー、Ctrl/Meta、IME/229/AltGraph、入力/モーダル/長押し抑止、競合/予約キー、解除、別キー保存、不正設定と保存失敗を検証。
- 実操作: 隔離したローカル4193の記入例で1/2/3/0の判断、Undo/Redo、F2編集、Shift+N兄弟追加、Nボード下書き→確定、V切替を確認。入力欄でn/1を打っても判断とカード数を変えない。
- キー変更: 採用をA、編集をEへ変更し、重複拒否、保存/再読込後の反映、古い1/F2の無効化、全体オフ、初期復元を確認。複数選択は2枚の選択状態と案内を確認。閲覧専用デモは判断変更/追加キーを拒否。
- 視覚: PCの設定画面をdocs/v1.01-shortcuts.jpgへ保存。行一覧をスクロールしても保存ボタンは画面内に残す。390幅はダイアログclientWidth/scrollWidthとも351px、保存ボタン下端795px<844px。実スマホ/IME/スクリーンリーダーとMac実機は未検証。
- 公開なし。以前の12枚ギャラリーはキー操作追加前の記録。今回の実装には独立したレビュー担当を使っていない。

# 応募条件の外部ライブラリ確認（2026-09-21）

- 対象: https://progedu.github.io/webappcontest/2026/summer/index.html の HTML/CSS/JavaScript・外部ライブラリ制限。応募資格全体の審査ではない。
- 作品本体: HTML/CSS/JavaScriptと標準DOM/SVG APIによる実装。package.jsonにdependencies/devDependenciesなし。公開用モジュールのimportは自作の相対ファイルのみ。外部CDNのアプリ用ライブラリなし。ビルドとテストはNode標準機能を使用する。
- 公開資産: 本番のCSS/JavaScript全12ファイルがHTTP 200で、改行を正規化した内容がローカルと一致。公開HTMLに埋め込んだ作品のstyle/scriptも一致。
- 配信側の例外: 公開HTMLにはCloudflare JavaScript Detectionsのコードが自動挿入され、/cdn-cgi/challenge-platform/scripts/jsd/main.jsを読み込む。これはボット対策の配信側処理であり、作品の表示・操作を作る外部ライブラリではない。ただし主催者がこの配信側処理の扱いを明示した記述は確認していない。公式説明: https://developers.cloudflare.com/cloudflare-challenges/challenge-types/javascript-detections/
- ロジック確認: static.test.mjs / build.test.mjs の9テスト成功。ライブラリを除去する実装修正は不要と判断。今回は画面の見た目を変更しておらず、視覚・実機テストおよび再デプロイは行っていない。
