{
  "name": "think-tree-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && node server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
