# 思考の芽 — 提出版の設計

## 合意 (2026-09-13)

サークルのハッカソン用の思考ノートを、ZEN StudyプログラミングコンテストのWebページ部門向けに作る。MCP接続を必須にしないブラウザ単独版を先に仕上げる方針に、オーナーが「それでいいと思う」と合意した。元の `hackathon-practices/think-tree/` と `decision-trail/` は変更・削除しない。

中心は、考えを抽象から具体へ枝分かれさせ、人間自身が選択して深めること。AIは任意。AIの提案がそのまま本体へ入ることはない。

## 実装上の選択

HTML / CSS / JavaScriptのみ。外部依存は実行時・テスト時ともにゼロ。静的ホスティングで動かし、ローカルNodeサーバーは開発補助に限る。

元の試作の紙・緑系のトークンを継承する。大きな紹介文を置かず、ノート一覧・思考ツリー・編集欄を主画面にする。スマホ幅ではノート一覧を引き出し、編集欄を下から開く。主な追加・編集操作は画面下部に配置する。

データはブラウザ保存。今回の明示合意に従いクラウド・認証は導入しない。公開URLを変えると保存領域も変わることをヘルプとREADMEに記載する。読み込みは追加式とし、元のノートを上書きしない。

## データと不変条件

`workspace = { version: 1, activeId, notebooks }`。

ノートは一つの根と、ID・親ID・表示順・考え・メモ・状態・出典を持つノード群。根の文章をそのままノート名に使い、二重管理しない。1〜30冊、各ノート1〜500ノード、最大深さ32。文章は240文字、メモは4000文字まで。

ID重複、孤立ノード、循環、複数の根、範囲外の状態・長さを拒否する。入力値はDOMのtextContent・valueへ設定し、HTMLとして解釈しない。UIの変更はスナップショットを持つトランザクションで包む。直近40操作の取り消しは、現在ページ内で有効。

localStorageには世代IDと全ワークスペースを一つの値で保存する。直前の保存を別キーにも置く。Web Locksが利用できるブラウザでは同一オリジンの保存を直列化し、世代比較で古いタブの上書きを拒否する。保存失敗・破損時はデータを消さず、入力を保持して書き出しを案内する。破損データの自動修復・強制上書きはしない。

## AI案の扱い

依頼文の対象は、選択した枝と子孫が既定。祖先の見出しを文脈として含めるが、他の枝のメモは含めない。全ノートを渡す範囲も本人が選べる。AIダイアログを開いた時点の対象を固定し、別の提案を承認して選択が動いても相談先を変えない。

受け取り形式は `{ version: 1, notebookId, parentId, branches: [{text, note}] }`。1〜12案を検証して承認待ちへ追加する。別ノート、消えた追加先、形式違反を拒否。同一内容の再取り込みは重複追加しない。

画面で承認した案だけを新しい枝にする。出典はAIとして残し、本人が文章を編集したら混合として表示する。「案をツリーに加える承認」と「その考えを最終的に採用する判断」は区別し、新規の枝は考え中で始める。

任意のWebMCPはブラウザネイティブ機能の有無を確認してから登録する。ツリーの限定読取と提案の保留だけを公開し、承認ツールは提供しない。未対応・登録失敗時もコピーと貼り付けの経路は残す。従来のMCP SDK接続を実装したとは表現しない。

## 今回入れないもの

モデルAPIへの直接呼び出し、クラウド同期、共同編集、ログイン、AIによる自動採否、キャンバス上の自由配置、PWAインストール。公開前のフィードバック窓口と実機検証はPLANの残件であり、完了扱いしない。
