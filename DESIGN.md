# 思考の芽 — 提出版の設計

## スマホ縦だけ下部編集（2026-09-20）

表示方式の切替条件は`(max-width:600px) and (orientation:portrait)`に統一。それ以外（695px幅、タブレット縦、スマホ横を含む）は右編集欄で、幅min(360px,44vw)をボードの横に予約する。スマホ縦だけ85dvhの下部モーダルとし、背景操作を止める。追加バーとmobile-onlyの補助操作、フォーカス制限も同じ条件で切替。文字やカード密度のための幅別調整は別に残す。

回転/リサイズで開いている編集カードや入力を消さない。ノート一覧が開いていれば先に閉じ、二重に背景操作を無効化しない。スマホ縦へ切替時、フォーカスが編集欄外なら×へ移す。狭い右パネル時は内側余白とボード操作バーを小さくし、保存表示と追加バーの重なりを防ぐ。

## 編集パネルの固定見出し（2026-09-20）

見出しと×はパネル上端の固定行。本文だけを兄弟のinspector-body内でスクロールする。外枠のpadding・sticky補正で位置を作らない。配置CSSはboard.cssへ集約。左右/下部の条件は上記「スマホ縦だけ下部編集」に従う。カード変更時は本文スクロールを先頭へ戻す。回帰確認はdocs/inspector-regression.mdとscripts/check-inspector-layout.mjs。共通CSS/画面幅/編集欄を変えた場合も必ず実行する。

## 背景濃度の確定（2026-09-20、Aを採用）

本人が実画面で比較してAを選択。点方眼はmuted色15%に固定し、24px間隔・半径1.15pxは維持。比較用のバー・CSS・JS・初回表示の例外を撤去。旧`?background=preview&dots=...`でも濃さはAで統一する。ノート内容・Undo・個人設定は変更しない。

## ドット方眼・タブレット配置・枠名の直接編集（2026-09-20）

ボードの背景を24画面px間隔の点方眼へ。muted色15%（本人がAを採用）、半径1.15px。線は置かない。追加バーは600px以下だけ既存のスマホ用幅広表示、601px以上はPCと同じ左下のコンパクト表示。編集パネルの940px境界は今回変更しない。

枠名のクリック/タップまたはキーボードEnterで、名前の位置に80文字までの入力欄を開く。Enter/フォーカス離脱で確定、Escapeで元へ戻す。空欄Enterでは入力を維持して案内し、空欄のまま外へ移れば元の名前に戻す。IME中は確定/取消/別操作を止める。名前は既存transaction経由で1回の履歴・自動保存へ。未確定は保存状態と離脱警告へ反映する。blur確定で隣のボタンのクリックを失わないよう、保存時の全体再描画を省略して枠ラベルだけ更新する。

名前をドラッグした場合は従来の枠移動を維持し、含まれるカードも追従。閲覧専用は名前の選択だけで入力欄を出さない。

## 四つ角からのサイズ変更（2026-09-20）

カードと分類枠は四つ角すべてでリサイズ可能。角に常設の四角いマークを置かず、PCでは透明な24画面pxの操作領域と対角線方向のカーソルで示す。枠の右上編集ボタンとは領域を離す。Tabで角を選べ、矢印で24px、Shift併用で80px移動。キーボードフォーカス時だけ輪郭を出す。

ドラッグ中は反対の角を固定し、最小/最大寸法でも反転しない。カードの既定224×116、最小160×96、最大2000×2000。任意のsize:{width,height}を既存ノードへ保存し、未指定は従来寸法。枠の最小280×200は維持。サイズ変更は1操作1履歴、Escape/pointercancel/blurで未確定操作を取り消す。閲覧専用では角を生成しない。

カードの表示行数・線の端・全体表示・追加時の空き位置・移動整列・分類枠の内包判定は実寸を使う。枠のリサイズでカード自体は移動しない。角から動かすのはそのカード1枚だけ。タッチは44画面px、カードの角領域は幅/高さ40%以内に制限し、70%未満の概観ではタッチ用リサイズを隠して本文の選択/移動を残す。実機タッチの体感は未確認。

## Ver1.01 キー操作（2026-09-20追加）

カード1枚の選択時に1=採用、2=保留、3=見送り、0=考え中、N=子追加、Shift+N=兄弟追加。F2=編集、F=ボード全体、V=広げる/比較の切替、Ctrl/⌘+Z=取り消し、Ctrl/⌘+Shift+Z=やり直し。初期設定のやり直しにはCtrl/⌘+Yも残す。ボードは直接入力の下書き、比較/一覧は入力ダイアログで追加する。根に兄弟は作れない。比較のカード余白クリックとTabでも対象を選べるようにする。判断変更は既存transactionによる自動保存と取り消しを使い、同じ判断への操作は履歴を増やさない。

「キー操作」・ノートメニュー・ヘルプから一覧/変更/個別解除/全体無効化/初期設定へ戻すを提供。「変更」後に実際のキーを押して割り当て、「設定を保存」で反映。重複はエラー、保存せず閉じれば変更しない。設定はthink-tree-shortcuts-v1に独立保存し、ノートJSONとUndoには含めない。保存不可では旧設定を維持し、読込不可では初期設定と警告を出す。

入力・select・contenteditable・IME・キー長押し・ダイアログ・ノート一覧・スマホ編集欄・ドラッグ/下書き中はコマンドを抑止。枠/複数選択には単一カードのコマンドを適用せず、閲覧専用では書き込みを行わない。Tab/Enter/Escape/矢印は既存の文脈操作として固定。ブラウザとOSの共通操作に衝突するキーは設定不可。PCは英数字/F2/Shift併用を中心とし、Ctrl/⌘はZ/YまたはShift+数字、AltはShift+英数字に限定する。

## 合意 (2026-09-13)

サークルのハッカソン用の思考ノートを、ZEN StudyプログラミングコンテストのWebページ部門向けに作る。MCP接続を必須にしないブラウザ単独版を先に仕上げる方針に、オーナーが「それでいいと思う」と合意した。元の `hackathon-practices/think-tree/` と `decision-trail/` は変更・削除しない。

中心は、考えを抽象から具体へ枝分かれさせ、人間自身が選択して深めること。AIは任意。AIの提案がそのまま本体へ入ることはない。

## 実装上の選択

2026-09-15のカード編集改善: 選択中の表示は緑の枠と読み上げ用aria-pressedで示し、カード右下の「選択中」は削除。PCマウスのダブルクリック（F2も維持）で既存の考え・メモ・判断の編集欄を右側へ開く。941px以上では360px幅を予約し、ボードと編集欄を並べる。別カードの単クリックで内容だけ切り替え、余白クリックやパン・ズームでは閉じない。カード切替時は編集欄を先頭へ戻す。PC選択時の自動拡大を止め、比較中の視点を保つ。ノート一覧は従来のモーダル引き出しを維持。スマホのダブルタップ追加と下部編集欄は維持し、縮幅時も背景無効化を揃える。保存形式・自動保存・IME未確定入力ガード・共有デモの読取専用は維持。

2026-09-15の初期表示改善: 表示・保存ロジックを変えず、CSS3本と起動JSを配布HTMLへインライン化し、依存6モジュールをmodulepreloadする。遅い独立リクエストを初期表示経路から除く。HTMLの編集元はsrc/index.html、dist/index.htmlはNode標準機能のnpm run buildで生成する。その他のCSS/JS編集元と静的ホスティング、外部依存ゼロは維持。根拠と計測条件はPERFORMANCE.md。

### ボード化の合意 (2026-09-13)

初版の縦リストを主画面にせず、カードと接続線が空間に広がるボードへ変更する。スマホはカードのダブルタップで子を一つ追加。PCはカードの四辺の追加点をクリックし、その側へ子を追加する。同じ親から繰り返すと兄弟、子から繰り返すとさらに深い枝になる。ホバーだけでは作成しない。

カードは移動でき、追加時は既存カードを動かさず空いた位置に配置する。線の色は利用者が自由に選ぶ分類で、採用・保留などの判断状態と連動させない。名前付きの四角い分類枠でカードと枝を囲み、枠の移動では含まれるカードも動く。分類枠は親子関係を変更しない。既存の保存・取り込み・人間の承認制を維持する。

実装上の選択: 枠は選んだ枝／複数選択を囲む入口を用意し、サイズ変更で含むカードを調整する。PCのShift選択とスマホの複数選択モードを併設。保存は従来の形式への任意フィールド追加で読み込み互換を保つ。旧一覧は読みやすさ・キーボード利用の副表示として残す。

HTML / CSS / JavaScriptのみ。外部依存は実行時・テスト時ともにゼロ。静的ホスティングで動かし、ローカルNodeサーバーは開発補助に限る。

元の試作の紙・緑系のトークンを継承する。大きな紹介文を置かず、思考ボードを主画面にする。PCの編集欄は必要時に右から開き、スマホ幅ではノート一覧を引き出し、編集欄を下から開く。主な追加・編集操作は画面下部に配置する。従来の階層リストは「一覧で読む」で残す。

データはブラウザ保存。今回の明示合意に従いクラウド・認証は導入しない。公開URLを変えると保存領域も変わることをヘルプとREADMEに記載する。読み込みは追加式とし、元のノートを上書きしない。

## データと不変条件

2026-09-14の配置修正: 戻す/やり直す矢印は上部バーから外し、倍率調整と同じ高さのボード左上に小さく配置する。ノート入口は上部バーの一番右に置き、右側から一覧を開く。左下の「同じ階層に足す」は削除し、「この先に考えを足す」と保存表示は維持する。旧一覧のEnter操作やスマホ編集欄の兄弟追加経路までは削除しない。履歴は一覧表示でも使えるよう、非表示になる倍率操作とは別の要素にする。AI連携の方式は変えず、依頼文のコピーと提案JSONの貼り戻し・人間の承認で動く。

2026-09-13追加合意: 左上に戻す/やり直す矢印を常設。直近40段階をページ内で保持し、新しい編集では進む履歴を破棄する。文章はフィールド別、600msの休止または約1秒の連続入力で区切る。カード/枠の移動はドラッグ1回単位。ボードのパン・ピンチはジェスチャー単位、ホイールズームは短い連続操作単位で、内容とは別種の表示履歴として保持し、共有デモでも表示だけ戻せる。リロード後の履歴は保持しない。

2026-09-13追加合意: PCホイールを上下移動からカーソル中心のズームへ変更。トラックパッドのピンチは小数のdeltaを保持し、感度を旧0.008から0.003へ下げる。通常ホイールは0.001。行/ページ単位をピクセル相当へ補正し、大きな1イベントは100px相当で制限。倍率自体を段階へ丸めない。＋／−は従来の1.2倍から1.05倍へ細分化。移動は余白ドラッグを維持し、入力欄のホイールは捕捉しない。

### 提出直前の画面・共有方針

2026-09-13の追加依頼で、ノート以外の固定領域を減らし、常設サイドバーを全幅で引き出しへ変更。ヘッダーは約56px、ボードは残りの高さを使う。保存状態・拡大操作・追加ボタンは小さく重ね、メニュー開閉ではカード座標を変えない。

本人がDB利用可能なら共有3枠を希望したが、期限を優先し、今回の追加返答で「固定デモを共有中として見せ、一般ノートは共有未対応と明示」に合意した。`?demo=origin` は独立した閲覧専用workspaceを使い、ブラウザの既存ノートを保存・上書きしない。コピー時だけ既存workspaceへ別IDで追加し、保存成功後に完了を表示する。保存復旧待ちではコピーを作らない。25カード・6分類枠・7段階は本人の課題と会話からの再構成であり、実入力履歴ではない。本人の許可で補った比較案はメモで明示。DB、端末識別、一般ノートの共有3枠は未実装で、禁止と判断したわけではない。

`workspace = { version: 1, activeId, notebooks }`。

ノートは一つの根と、ID・親ID・表示順・考え・メモ・状態・出典を持つノード群。根の文章をそのままノート名に使い、二重管理しない。1〜30冊、各ノート1〜500ノード、最大深さ32。文章は240文字、メモは4000文字まで。

空間情報はノードの任意フィールド `position:{x,y}` / `branchSide` / `lineColor` とノートの `frames` に追加する。旧データでは未定義を受け入れ、初回描画前に位置と既定色を補う。データのversionと保存キーを変えず、既存の世代比較・バックアップを維持する。枠はID・名前・座標・幅高・色・nodeIdsを持ち、上限60個。ID・座標・色・含むノードを読み込み時に検証し、JSONコピー時には枠IDと含むノードIDも振り直す。

ドラッグとサイズ変更の途中は表示専用の位置で描き、指を離してから一つの履歴として確定する。操作取消・pointercancel・フォーカス喪失では表示上の移動を破棄し、保存データは変えない。分類枠の作成・移動・カード追加／移動・枠のサイズ変更後に、完全に囲まれたカードへ全枠の所属を更新する。枠の見出し部分は内包範囲に含めない。保存・読み込み時は幾何学的内包とnodeIdsが一致することを検証し、不一致のJSONは拒否する。複数の枠が重なったときにも、親子や判断を変更しない。

追加中の空カードは表示専用の下書きで、文章を入力してEnter／追加、または別操作へ移る時に確定する。空欄は取り消す。入力中は未確定表示、文章を残したままページを閉じる時は警告する。日本語変換中の編集対象では別操作を拒否し、変換確定を案内する。複数カード／枠の選択中は単一カードの編集を無効化する。再描画では論理IDに対応する操作先へフォーカスを戻す。AI提案は従来どおり個別承認後にだけ実ノードを作り、空き位置へ配置する。

ID重複、孤立ノード、循環、複数の根、範囲外の状態・長さを拒否する。入力値はDOMのtextContent・valueへ設定し、HTMLとして解釈しない。UIの変更はスナップショットを持つトランザクションで包む。直近40操作の取り消しは、現在ページ内で有効。

localStorageには世代IDと全ワークスペースを一つの値で保存する。直前の保存を別キーにも置く。Web Locksが利用できるブラウザでは同一オリジンの保存を直列化し、世代比較で古いタブの上書きを拒否する。保存失敗・破損時はデータを消さず、入力を保持して書き出しを案内する。破損データの自動修復・強制上書きはしない。

## AI案の扱い

依頼文の対象は、選択した枝と子孫が既定。祖先の見出しを文脈として含めるが、他の枝のメモは含めない。全ノートを渡す範囲も本人が選べる。AIダイアログを開いた時点の対象を固定し、別の提案を承認して選択が動いても相談先を変えない。

受け取り形式は `{ version: 1, notebookId, parentId, branches: [{text, note}] }`。1〜12案を検証して承認待ちへ追加する。別ノート、消えた追加先、形式違反を拒否。同一内容の再取り込みは重複追加しない。

画面で承認した案だけを新しい枝にする。出典はAIとして残し、本人が文章を編集したら混合として表示する。「案をツリーに加える承認」と「その考えを最終的に採用する判断」は区別し、新規の枝は考え中で始める。

任意のWebMCPはブラウザネイティブ機能の有無を確認してから登録する。ツリーの限定読取と提案の保留だけを公開し、承認ツールは提供しない。未対応・登録失敗時もコピーと貼り付けの経路は残す。従来のMCP SDK接続を実装したとは表現しない。

## 今回入れないもの

モデルAPIへの直接呼び出し、クラウド同期、共同編集、ログイン、AIによる自動採否、PWAインストール。キャンバスの自由配置はボード化の合意により追加対象へ変更した。公開前のフィードバック窓口と実機検証はPLANの残件であり、完了扱いしない。

## 移動時の整列補助（2026-09-16）

カード・複数選択カード・分類枠のドラッグでは、他の表示カード/枠の端同士または中心同士に画面上6px以内まで近づくと吸着し、赤い破線を最大縦横1本ずつ表示する。複数選択は全体の外接矩形、枠は外周を基準とし、中身の相対位置を維持する。動かしている要素・その中身・動かすカードを含む枠は参照対象から除く。ズーム倍率に応じて判定幅と線幅を補正し、Altドラッグでは吸着とガイドを解除する。

表示はドラッグ中だけで、確定/取消で消す。最終ポインター位置から補正し、既存の1ドラッグ1履歴・保存/取消へつなぐ。サイズ変更・キーボード移動・共有デモのパンには吸着しない。保存形式は変更しない。

## ソースの保守（2026-09-14）

本人のコードの読みやすさ・コメント整備の依頼により、保存・履歴・共有デモの境界に説明を加え、JavaScriptをPrettier 3.6.2で整形する。
保守の入口と検証手順はMAINTAINING.mdにまとめる。
整形ツールは開発時に任意で使うもので、アプリ実行時・テスト時の依存ゼロは維持する。
この整理では機能・保存形式・HTML・CSSを変更せず、過去のコミット日時や履歴を書き換えない。

## Ver1.01（2026-09-20公開）

本人の約30分の自主改善依頼に基づき、用途を「迷っている案を広げ、比べて決め、理由を持ち帰る」へ明確化。競合に自由配置/AI/判断記録があることを公式情報で確認し、独自機能の不存在主張ではなく設定不要の一続きの操作を差別化仮説とした。根拠はdocs/v1.01-competition.md。

新しいcompare表示は、選択ノードに子があればその直下、葉なら同じ親の直下を候補にする。比較中は問いIDを保持し、編集対象を選んでも比較対象は動かさない。問いを切り替えると旧編集欄を閉じる。選択肢を追加する際は追加先IDを独立に持ち、取消で選択を変えない。compare/boardの表示種別だけをURL viewに反映する。ノート内容はURLへ入れない。

状態とnoteは既存のデータを再利用し、transaction/history/storage境界を維持。新しい保存schemaは導入しない。持ち帰りは現在の問い・直下の候補を状態ごとにまとめたMarkdown。noteはメモ・判断理由として扱い、AIによる理由補完はしない。clipboard失敗時は本文を選択して手動コピーを案内する。保存ファイルも同じ本文。

初回の保存データなし・復旧ブロックなしの場合だけ、具体例のノートを作る。既存ノートはそのまま読み込む。例から試す操作は新しいノートを追加し、同一セッションで作った例は再利用する。共有デモは別workspace・readOnlyを維持。PCは既存右パネル、940px以下はモーダル編集欄と背景inert、キーボード復帰を維持する。

当初は本人指示に従いローカルbranchとport4191で確認。9月20日の公開指示でショートカットまで公開済み。UIの試し方と変更順はVERSION-1.01.md、現在の配布状態はLAUNCH-STATE.md。
